#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
PYTHON="$ROOT/.ai/runtime/.venv/bin/python"
if [[ -x "$PYTHON" ]]; then
  exec "$PYTHON" -m ai_core.cli "$@"
fi
exec uv run --project "$ROOT/.ai/runtime" ai "$@"
