from __future__ import annotations

# Lightweight doctor metadata. Keep this synchronized with mcp_server.TOOLS;
# the MCP server regression suite enforces exact parity whenever tools change.
MCP_METHOD_COUNT = 61