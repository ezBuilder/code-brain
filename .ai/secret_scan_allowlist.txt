# Code Brain secret_scan acknowledgment list.
# One repo-relative path per line. Comments start with '#'. Blank lines ignored.
# Entries listed here are still scanned, but matches are reported as
# "acknowledged" (not flagged as unauthorized) by `ai doctor`.
#
# Use this for known-safe files in the host repo that pattern-match secret
# heuristics (e.g. example tokens in docs, fixture data with placeholder keys).
# Do NOT add real secret-bearing files here — encrypt them under
# .ai/secrets/*.enc.yaml instead.
#
# Example:
# docs/examples/api-call.md
# tests/fixtures/sample-token-fixture.json
kits/global-agent-kit/scripts/evolve-capture.sh
.ai/runtime/tests/test_posttooluse_wire.py
